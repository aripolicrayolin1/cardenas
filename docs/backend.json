
{
  "entities": {
    "UserProfile": {
      "title": "Perfil de Usuario",
      "description": "Información detallada del agricultor vinculada a su cuenta de AgroTech Hidalgo.",
      "type": "object",
      "properties": {
        "displayName": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "photoURL": { "type": "string" },
        "location": { "type": "string" },
        "cropType": { "type": "string" }
      },
      "required": ["email"]
    },
    "Farm": {
      "title": "Finca Agrícola",
      "description": "Datos de un terreno monitoreado y gestionado por el usuario en la región.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "location": { "type": "string" },
        "crop": { "type": "string" },
        "area": { "type": "string" },
        "userId": { "type": "string" },
        "status": { "type": "string" },
        "humidity": { "type": "string" },
        "temp": { "type": "string" },
        "lastUpdate": { "type": "string" }
      },
      "required": ["name", "userId"]
    }
  },
  "auth": {
    "providers": ["google.com", "password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Datos privados de perfil. Solo el usuario dueño ({userId}) tiene acceso total."
    },
    "/users/{userId}/farms/{farmId}": {
      "schema": "Farm",
      "description": "Colección de fincas privadas. Solo el usuario autenticado que coincida con {userId} puede listar, leer y escribir."
    }
  }
}
